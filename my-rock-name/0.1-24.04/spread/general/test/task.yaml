summary: test the 'normal-rock' project

execute: |
  docker run --rm normal-rock:0.1 exec zstdcat /var/lib/chisel/manifest.wall | jq -r 'select(.kind == "package" and .name == "hello") | .version' | grep 2.10-3build1
